PATH

printenv                                        # list environmental variables with values

export PATH="${PATH}:/home/my_user/bin"         # add a directory to the PATH for local usage (put in ~/.bash_profile)
